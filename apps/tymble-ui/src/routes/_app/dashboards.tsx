import { createFileRoute } from '@tanstack/react-router';

export const Route = createFileRoute('/_app/dashboards')({
  component: RouteComponent,
});

function RouteComponent() {
  return (
    <div>
      This change removes the maximum width constraint, which might be causing
      the horizontal scrollbar when the edit form is displayed. After you make
      this change, please save the file. The Vite dev server should apply the
      changes, and the scrollbar issue might be resolved. This change removes
      the maximum width constraint, which might be causing the horizontal
      scrollbar when the edit form is displayed. After you make this change,
      please save the file. The Vite dev server should apply the changes, and
      the scrollbar issue might be resolved. This change removes the maximum
      width constraint, which might be causing the horizontal scrollbar when the
      edit form is displayed. After you make this change, please save the file.
      The Vite dev server should apply the changes, and the scrollbar issue
      might be resolved. This change removes the maximum width constraint, which
      might be causing the horizontal scrollbar when the edit form is displayed.
      After you make this change, please save the file. The Vite dev server
      should apply the changes, and the scrollbar issue might be resolved. This
      change removes the maximum width constraint, which might be causing the
      horizontal scrollbar when the edit form is displayed. After you make this
      change, please save the file. The Vite dev server should apply the
      changes, and the scrollbar issue might be resolved. This change removes
      the maximum width constraint, which might be causing the horizontal
      scrollbar when the edit form is displayed. After you make this change,
      please save the file. The Vite dev server should apply the changes, and
      the scrollbar issue might be resolved. This change removes the maximum
      width constraint, which might be causing the horizontal scrollbar when the
      edit form is displayed. After you make this change, please save the file.
      The Vite dev server should apply the changes, and the scrollbar issue
      might be resolved. This change removes the maximum width constraint, which
      might be causing the horizontal scrollbar when the edit form is displayed.
      After you make this change, please save the file. The Vite dev server
      should apply the changes, and the scrollbar issue might be resolved. This
      change removes the maximum width constraint, which might be causing the
      horizontal scrollbar when the edit form is displayed. After you make this
      change, please save the file. The Vite dev server should apply the
      changes, and the scrollbar issue might be resolved. This change removes
      the maximum width constraint, which might be causing the horizontal
      scrollbar when the edit form is displayed. After you make this change,
      please save the file. The Vite dev server should apply the changes, and
      the scrollbar issue might be resolved. This change removes the maximum
      width constraint, which might be causing the horizontal scrollbar when the
      edit form is displayed. After you make this change, please save the file.
      The Vite dev server should apply the changes, and the scrollbar issue
      might be resolved. This change removes the maximum width constraint, which
      might be causing the horizontal scrollbar when the edit form is displayed.
      After you make this change, please save the file. The Vite dev server
      should apply the changes, and the scrollbar issue might be resolved. This
      change removes the maximum width constraint, which might be causing the
      horizontal scrollbar when the edit form is displayed. After you make this
      change, please save the file. The Vite dev server should apply the
      changes, and the scrollbar issue might be resolved. This change removes
      the maximum width constraint, which might be causing the horizontal
      scrollbar when the edit form is displayed. After you make this change,
      please save the file. The Vite dev server should apply the changes, and
      the scrollbar issue might be resolved. This change removes the maximum
      width constraint, which might be causing the horizontal scrollbar when the
      edit form is displayed. After you make this change, please save the file.
      The Vite dev server should apply the changes, and the scrollbar issue
      might be resolved. This change removes the maximum width constraint, which
      might be causing the horizontal scrollbar when the edit form is displayed.
      After you make this change, please save the file. The Vite dev server
      should apply the changes, and the scrollbar issue might be resolved. This
      change removes the maximum width constraint, which might be causing the
      horizontal scrollbar when the edit form is displayed. After you make this
      change, please save the file. The Vite dev server should apply the
      changes, and the scrollbar issue might be resolved. This change removes
      the maximum width constraint, which might be causing the horizontal
      scrollbar when the edit form is displayed. After you make this change,
      please save the file. The Vite dev server should apply the changes, and
      the scrollbar issue might be resolved. This change removes the maximum
      width constraint, which might be causing the horizontal scrollbar when the
      edit form is displayed. After you make this change, please save the file.
      The Vite dev server should apply the changes, and the scrollbar issue
      might be resolved. This change removes the maximum width constraint, which
      might be causing the horizontal scrollbar when the edit form is displayed.
      After you make this change, please save the file. The Vite dev server
      should apply the changes, and the scrollbar issue might be resolved. This
      change removes the maximum width constraint, which might be causing the
      horizontal scrollbar when the edit form is displayed. After you make this
      change, please save the file. The Vite dev server should apply the
      changes, and the scrollbar issue might be resolved. This change removes
      the maximum width constraint, which might be causing the horizontal
      scrollbar when the edit form is displayed. After you make this change,
      please save the file. The Vite dev server should apply the changes, and
      the scrollbar issue might be resolved. This change removes the maximum
      width constraint, which might be causing the horizontal scrollbar when the
      edit form is displayed. After you make this change, please save the file.
      The Vite dev server should apply the changes, and the scrollbar issue
      might be resolved. This change removes the maximum width constraint, which
      might be causing the horizontal scrollbar when the edit form is displayed.
      After you make this change, please save the file. The Vite dev server
      should apply the changes, and the scrollbar issue might be resolved. This
      change removes the maximum width constraint, which might be causing the
      horizontal scrollbar when the edit form is displayed. After you make this
      change, please save the file. The Vite dev server should apply the
      changes, and the scrollbar issue might be resolved. This change removes
      the maximum width constraint, which might be causing the horizontal
      scrollbar when the edit form is displayed. After you make this change,
      please save the file. The Vite dev server should apply the changes, and
      the scrollbar issue might be resolved. This change removes the maximum
      width constraint, which might be causing the horizontal scrollbar when the
      edit form is displayed. After you make this change, please save the file.
      The Vite dev server should apply the changes, and the scrollbar issue
      might be resolved. This change removes the maximum width constraint, which
      might be causing the horizontal scrollbar when the edit form is displayed.
      After you make this change, please save the file. The Vite dev server
      should apply the changes, and the scrollbar issue might be resolved. This
      change removes the maximum width constraint, which might be causing the
      horizontal scrollbar when the edit form is displayed. After you make this
      change, please save the file. The Vite dev server should apply the
      changes, and the scrollbar issue might be resolved. This change removes
      the maximum width constraint, which might be causing the horizontal
      scrollbar when the edit form is displayed. After you make this change,
      please save the file. The Vite dev server should apply the changes, and
      the scrollbar issue might be resolved. This change removes the maximum
      width constraint, which might be causing the horizontal scrollbar when the
      edit form is displayed. After you make this change, please save the file.
      The Vite dev server should apply the changes, and the scrollbar issue
      might be resolved. This change removes the maximum width constraint, which
      might be causing the horizontal scrollbar when the edit form is displayed.
      After you make this change, please save the file. The Vite dev server
      should apply the changes, and the scrollbar issue might be resolved. This
      change removes the maximum width constraint, which might be causing the
      horizontal scrollbar when the edit form is displayed. After you make this
      change, please save the file. The Vite dev server should apply the
      changes, and the scrollbar issue might be resolved. This change removes
      the maximum width constraint, which might be causing the horizontal
      scrollbar when the edit form is displayed. After you make this change,
      please save the file. The Vite dev server should apply the changes, and
      the scrollbar issue might be resolved. This change removes the maximum
      width constraint, which might be causing the horizontal scrollbar when the
      edit form is displayed. After you make this change, please save the file.
      The Vite dev server should apply the changes, and the scrollbar issue
      might be resolved. This change removes the maximum width constraint, which
      might be causing the horizontal scrollbar when the edit form is displayed.
      After you make this change, please save the file. The Vite dev server
      should apply the changes, and the scrollbar issue might be resolved. This
      change removes the maximum width constraint, which might be causing the
      horizontal scrollbar when the edit form is displayed. After you make this
      change, please save the file. The Vite dev server should apply the
      changes, and the scrollbar issue might be resolved.
    </div>
  );
}
