generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}

// Example model (optional). Uncomment and migrate when ready.
// model Profile {
//   id        String   @id @default(uuid())
//   createdAt DateTime @default(now())
//   email     String   @unique
//   displayName String?
// }
