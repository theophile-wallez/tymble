{
  "name": "tymble",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "pnpm -w run dev:db && turbo run dev",
    "lint": "turbo run lint",
    "lint:fix": "turbo run lint:fix",
    "check-types": "turbo run check-types"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.4",
    "turbo": "^2.6.0",
    "typescript": "5.9.2",
    "ultracite": "6.3.2"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  }
}
