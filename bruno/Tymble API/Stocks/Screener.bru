meta {
  name: Screener
  type: http
  seq: 4
}

get {
  url: {{base_url}}/stocks/screener?scrIds={{scrIds}}
  body: none
  auth: bearer
}

query {
  scrIds: {{scrIds}}
}

auth:bearer {
  token: {{token}}
}

vars:pre-request {
  scrIds: day_gainers
}
