meta {
  name: Insight
  type: http
  seq: 2
}

get {
  url: {{base_url}}/stocks/insights?ticker={{ticker}}
  body: none
  auth: bearer
}

query {
  ticker: {{ticker}}
}

auth:bearer {
  token: {{token}}
}

vars:pre-request {
  ticker: "AAPL"
}
