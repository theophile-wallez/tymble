meta {
  name: Chart
  type: http
  seq: 3
}

get {
  url: {{base_url}}/stocks/chart?ticker={{ticker}}
  body: none
  auth: bearer
}

query {
  ticker: {{ticker}}
}

auth:bearer {
  token: {{token}}
}

vars:pre-request {
  ticker: AAPL
}
