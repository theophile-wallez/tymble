meta {
  name: Quote
  type: http
  seq: 5
}

get {
  url: {{base_url}}/stocks/quote?ticker={{ticker}}
  body: none
  auth: bearer
}

query {
  ticker: {{ticker}}
}

auth:bearer {
  token: {{token}}
}

vars:pre-request {
  ticker: ESE.PA
}
