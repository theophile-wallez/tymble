meta {
  name: Search
  type: http
  seq: 1
}

get {
  url: {{base_url}}/stocks/search?name={{name}}
  body: none
  auth: bearer
}

query {
  name: {{name}}
}

auth:bearer {
  token: {{token}}
}

vars:pre-request {
  name: bnp
}
