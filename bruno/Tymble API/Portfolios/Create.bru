meta {
  name: Create
  type: http
  seq: 1
}

post {
  url: {{base_url}}/portfolio
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "Trade Republic 3",
    "type": "CTO",
    "provider": "Trade Republic"
  }
}
